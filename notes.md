For finding the size of a data type in bytes, do `sizeof(<type>)`. To time code, use `#include <chrono>` and `std::chrono`. Then use the code to time the program [here](https://www.geeksforgeeks.org/cpp/measure-execution-time-function-cpp/).

To do next:
* Write naive operations for vectors and matrices (norms, inner product, matrix multiplication, etc)
* Optimize code to run in parallel and time it.